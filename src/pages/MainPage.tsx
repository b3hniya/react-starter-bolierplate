import { Button } from "antd";

const MainPage = () => {
  return (
    <div>
      <Button type="primary" className="font-bold">
        Main Page Button
      </Button>
    </div>
  );
};

export default MainPage;
